import React from 'react';
import { countires } from '../../constants';

const Home = () => {
    return (
        <>
            {/* {
                countires.map((e) => (
                    <div>
                        <p>{e.name}</p>
                    </div>
                ))
            } */}
            
        </>
    );
};

export default Home;